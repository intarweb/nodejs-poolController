# Fork tracking — intarweb/nodejs-poolController

This is a soft fork of [`tagyoureit/nodejs-poolController`](https://github.com/tagyoureit/nodejs-poolController). We track upstream HEAD and publish a HEAD-tracked image to GHCR with any open intarweb→upstream PRs cherry-picked on top.

> All forks we manage with the sync-upstream + from-source-build pattern live under the [`intarweb`](https://github.com/intarweb) GitHub org. See the `ghcr-fork-mirror` skill for the canonical recipe.

## Branch model

| Branch | Purpose | Source of truth |
|---|---|---|
| `master` | Upstream-clean tracking + ops overlay (canonical workflows + FORK_INFO.md) | upstream + us |
| `intarweb-dev` | Deploy track — `master` + cherry-pick of every open intarweb→upstream PR. Drives `:latest`. | us |
| `feat/*`, `fix/*` | Individual patch branches we PR upstream | us |

## Upstream sync

| Property | Value |
|---|---|
| Upstream | [`tagyoureit/nodejs-poolController`](https://github.com/tagyoureit/nodejs-poolController) |
| Upstream branch tracked | `master` |
| Sync cadence | Hourly (gated by `vars.SYNC_CADENCE_HOURS`) + `workflow_dispatch` + `fold-on-push` re-trigger on PR-branch push |
| Sync mechanism | Hard-reset `master` to `upstream/master`, re-apply ops overlay, regen `intarweb-dev` by cherry-picking every open intarweb→upstream PR |
| Sync workflow | [`.github/workflows/sync-upstream.yml`](.github/workflows/sync-upstream.yml) |

## Build pipeline

| Property | Value |
|---|---|
| Image | `ghcr.io/intarweb/njspc` |
| `:latest` source | Build-from-source on `intarweb-dev` (gated by `vars.IS_SOURCE_BUILT == 'true'`) |
| `:sha-<short>` | Every build |
| Tagged releases (`v*`, etc.) | Mirror upstream's released tags via [`fork-publish.yml`](.github/workflows/fork-publish.yml) |
| Build workflow | [`.github/workflows/build-from-source.yml`](.github/workflows/build-from-source.yml) |
| Smoke gate | Fail-closed on cherry-pick conflict — prior `:latest` keeps serving (honest-fact #65). |

## Local patches we carry on `intarweb-dev` (vs `upstream/master`)

<!-- intarweb:patches-start  auto-generated by .github/workflows/update-fork-info.yml — do not edit by hand -->

_No open intarweb→upstream PRs — `intarweb-dev` matches `upstream/master` exactly._

<!-- intarweb:patches-end -->

## How to consume

```yaml
# docker-compose.yml
services:
  njspc:
    image: ghcr.io/intarweb/njspc:latest
```

Pin to a specific build for reproducibility:

```yaml
    image: ghcr.io/intarweb/njspc:sha-<short>
```

## Maintenance recipes

**Manually re-sync onto upstream:**
```bash
gh workflow run "Sync from upstream + auto-regen intarweb-dev" --repo intarweb/nodejs-poolController
```

**Force a fresh build of `:latest` without an upstream change:**
```bash
gh workflow run "Build from source → GHCR" --repo intarweb/nodejs-poolController --ref intarweb-dev
```

**Add a new patch to intarweb-dev:**
```bash
git checkout -b feat/my-thing upstream/master
# ...do work...
git push origin feat/my-thing
gh pr create --repo tagyoureit/nodejs-poolController --base master --head intarweb:feat/my-thing
# Next sync-upstream tick auto-cherry-picks the new PR onto intarweb-dev.
```
